#include <stdio.h>

void border(unsigned char *buf, int width, int height) {

    int i;


    // top border
    for (i=0; i<width*5; i++) {
        buf[i] = 255; 
    }
  

    // bottom border

    // left border

    //right border
}